{
  "builds": [
    {
      "src": "src/main.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "PYTHONPATH=$PYTHONPATH:$PWD pip install -r requirements.txt && playwright install chromium"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.py"
    }
  ]
}